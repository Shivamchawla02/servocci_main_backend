// routes/adminRoutes.js
import express from "express";

const router = express.Router();

/* -----------------------------------
   ADMIN ROUTES (EMPTY FOR NOW)
----------------------------------- */

// Add actual admin-only routes here later.
// Example:
// router.get("/dashboard", adminAuth, getAdminStats);

export default router;
